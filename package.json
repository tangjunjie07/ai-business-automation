{
  "name": "ai-business-automation",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "echo 'Run individual app/package setup (create-next-app / expo init)'",
    "dev:web": "cd apps/web && npm run dev",
    "validate:ai-rules": "node ./scripts/validate_ai_rules.js"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.2.0",
    "bcryptjs": "^2.4.3",
    "postgres": "^3.4.7"
  }
}
