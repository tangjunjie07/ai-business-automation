{
  "name": "ai-business-automation",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "echo 'Run individual app/package setup (create-next-app / expo init)'",
    "dev:web": "cd apps/web && echo 'Run create-next-app in this folder'"
  }
}
