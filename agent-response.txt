{"summary":"チャットのスクロール主体をページ全体から message-list 側に移動し、入力エリア直上に常に最新メッセージが表示される構造に修正しました。three-col-layout の main から overflow-auto を削除し、message-list 内で overflow-y-auto によるスクロール管理を行います。","files":[{"path":"app/components/three-col-layout.tsx","content":"import { ReactNode } from \"react\";\n\ninterface ThreeColLayoutProps {\n  left?: ReactNode;\n  center: ReactNode;\n  right?: ReactNode;\n}\n\nexport function ThreeColLayout({ left, center, right }: ThreeColLayoutProps) {\n  return (\n    <div className=\"flex h-full w-full\">\n      {left && <aside className=\"w-64 border-r\">{left}</aside>}\n      <main className=\"flex flex-1 flex-col\">{center}</main>\n      {right && <aside className=\"w-80 border-l\">{right}</aside>}\n    </div>\n  );\n}\n"},{"path":"app/components/message-list.tsx","content":"import { useEffect, useRef } from \"react\";\nimport type { ChatMessage } from \"@/app/types/analysis\";\n\ninterface MessageListProps {\n  messages: ChatMessage[];\n}\n\nexport function MessageList({ messages }: MessageListProps) {\n  const containerRef = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    // 送信後の自動スクロールは保留（将来対応）\n  }, [messages]);\n\n  return (\n    <div\n      ref={containerRef}\n      className=\"flex-1 overflow-y-auto px-4 py-2\"\n    >\n      <div className=\"flex flex-col gap-3\">\n        {messages.map((message) => (\n          <div key={message.id}>{message.content}</div>\n        ))}\n      </div>\n    </div>\n  );\n}\n"}]}